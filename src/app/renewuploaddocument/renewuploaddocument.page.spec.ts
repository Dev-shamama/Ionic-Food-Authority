import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RenewuploaddocumentPage } from './renewuploaddocument.page';

describe('RenewuploaddocumentPage', () => {
  let component: RenewuploaddocumentPage;
  let fixture: ComponentFixture<RenewuploaddocumentPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RenewuploaddocumentPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
