import { CnicNumberMaskDirective } from './cnic-number-mask.directive';

describe('CnicNumberMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CnicNumberMaskDirective();
    expect(directive).toBeTruthy();
  });
});
